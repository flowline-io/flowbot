// Code generated by ent, DO NOT EDIT.

package gen

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/flowline-io/flowbot/internal/store/ent/gen/keyresult"
	"github.com/flowline-io/flowbot/internal/store/ent/gen/objective"
	"github.com/flowline-io/flowbot/internal/store/ent/gen/predicate"
	"github.com/flowline-io/flowbot/internal/store/ent/gen/review"
)

// ObjectiveQuery is the builder for querying Objective entities.
type ObjectiveQuery struct {
	config
	ctx            *QueryContext
	order          []objective.OrderOption
	inters         []Interceptor
	predicates     []predicate.Objective
	withKeyResults *KeyResultQuery
	withReviews    *ReviewQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ObjectiveQuery builder.
func (_q *ObjectiveQuery) Where(ps ...predicate.Objective) *ObjectiveQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ObjectiveQuery) Limit(limit int) *ObjectiveQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ObjectiveQuery) Offset(offset int) *ObjectiveQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ObjectiveQuery) Unique(unique bool) *ObjectiveQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ObjectiveQuery) Order(o ...objective.OrderOption) *ObjectiveQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryKeyResults chains the current query on the "key_results" edge.
func (_q *ObjectiveQuery) QueryKeyResults() *KeyResultQuery {
	query := (&KeyResultClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(objective.Table, objective.FieldID, selector),
			sqlgraph.To(keyresult.Table, keyresult.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, objective.KeyResultsTable, objective.KeyResultsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReviews chains the current query on the "reviews" edge.
func (_q *ObjectiveQuery) QueryReviews() *ReviewQuery {
	query := (&ReviewClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(objective.Table, objective.FieldID, selector),
			sqlgraph.To(review.Table, review.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, objective.ReviewsTable, objective.ReviewsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Objective entity from the query.
// Returns a *NotFoundError when no Objective was found.
func (_q *ObjectiveQuery) First(ctx context.Context) (*Objective, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{objective.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ObjectiveQuery) FirstX(ctx context.Context) *Objective {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Objective ID from the query.
// Returns a *NotFoundError when no Objective ID was found.
func (_q *ObjectiveQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{objective.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ObjectiveQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Objective entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Objective entity is found.
// Returns a *NotFoundError when no Objective entities are found.
func (_q *ObjectiveQuery) Only(ctx context.Context) (*Objective, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{objective.Label}
	default:
		return nil, &NotSingularError{objective.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ObjectiveQuery) OnlyX(ctx context.Context) *Objective {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Objective ID in the query.
// Returns a *NotSingularError when more than one Objective ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ObjectiveQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{objective.Label}
	default:
		err = &NotSingularError{objective.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ObjectiveQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Objectives.
func (_q *ObjectiveQuery) All(ctx context.Context) ([]*Objective, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Objective, *ObjectiveQuery]()
	return withInterceptors[[]*Objective](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ObjectiveQuery) AllX(ctx context.Context) []*Objective {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Objective IDs.
func (_q *ObjectiveQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(objective.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ObjectiveQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ObjectiveQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ObjectiveQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ObjectiveQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ObjectiveQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("gen: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ObjectiveQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ObjectiveQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ObjectiveQuery) Clone() *ObjectiveQuery {
	if _q == nil {
		return nil
	}
	return &ObjectiveQuery{
		config:         _q.config,
		ctx:            _q.ctx.Clone(),
		order:          append([]objective.OrderOption{}, _q.order...),
		inters:         append([]Interceptor{}, _q.inters...),
		predicates:     append([]predicate.Objective{}, _q.predicates...),
		withKeyResults: _q.withKeyResults.Clone(),
		withReviews:    _q.withReviews.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithKeyResults tells the query-builder to eager-load the nodes that are connected to
// the "key_results" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ObjectiveQuery) WithKeyResults(opts ...func(*KeyResultQuery)) *ObjectiveQuery {
	query := (&KeyResultClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withKeyResults = query
	return _q
}

// WithReviews tells the query-builder to eager-load the nodes that are connected to
// the "reviews" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ObjectiveQuery) WithReviews(opts ...func(*ReviewQuery)) *ObjectiveQuery {
	query := (&ReviewClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withReviews = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UID string `json:"uid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Objective.Query().
//		GroupBy(objective.FieldUID).
//		Aggregate(gen.Count()).
//		Scan(ctx, &v)
func (_q *ObjectiveQuery) GroupBy(field string, fields ...string) *ObjectiveGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ObjectiveGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = objective.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UID string `json:"uid,omitempty"`
//	}
//
//	client.Objective.Query().
//		Select(objective.FieldUID).
//		Scan(ctx, &v)
func (_q *ObjectiveQuery) Select(fields ...string) *ObjectiveSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ObjectiveSelect{ObjectiveQuery: _q}
	sbuild.label = objective.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ObjectiveSelect configured with the given aggregations.
func (_q *ObjectiveQuery) Aggregate(fns ...AggregateFunc) *ObjectiveSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ObjectiveQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("gen: uninitialized interceptor (forgotten import gen/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !objective.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("gen: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ObjectiveQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Objective, error) {
	var (
		nodes       = []*Objective{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withKeyResults != nil,
			_q.withReviews != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Objective).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Objective{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withKeyResults; query != nil {
		if err := _q.loadKeyResults(ctx, query, nodes,
			func(n *Objective) { n.Edges.KeyResults = []*KeyResult{} },
			func(n *Objective, e *KeyResult) { n.Edges.KeyResults = append(n.Edges.KeyResults, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withReviews; query != nil {
		if err := _q.loadReviews(ctx, query, nodes,
			func(n *Objective) { n.Edges.Reviews = []*Review{} },
			func(n *Objective, e *Review) { n.Edges.Reviews = append(n.Edges.Reviews, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ObjectiveQuery) loadKeyResults(ctx context.Context, query *KeyResultQuery, nodes []*Objective, init func(*Objective), assign func(*Objective, *KeyResult)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*Objective)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(keyresult.FieldObjectiveID)
	}
	query.Where(predicate.KeyResult(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(objective.KeyResultsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ObjectiveID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "objective_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ObjectiveQuery) loadReviews(ctx context.Context, query *ReviewQuery, nodes []*Objective, init func(*Objective), assign func(*Objective, *Review)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*Objective)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(review.FieldObjectiveID)
	}
	query.Where(predicate.Review(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(objective.ReviewsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ObjectiveID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "objective_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *ObjectiveQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ObjectiveQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(objective.Table, objective.Columns, sqlgraph.NewFieldSpec(objective.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, objective.FieldID)
		for i := range fields {
			if fields[i] != objective.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ObjectiveQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(objective.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = objective.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ObjectiveGroupBy is the group-by builder for Objective entities.
type ObjectiveGroupBy struct {
	selector
	build *ObjectiveQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ObjectiveGroupBy) Aggregate(fns ...AggregateFunc) *ObjectiveGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ObjectiveGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ObjectiveQuery, *ObjectiveGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ObjectiveGroupBy) sqlScan(ctx context.Context, root *ObjectiveQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ObjectiveSelect is the builder for selecting fields of Objective entities.
type ObjectiveSelect struct {
	*ObjectiveQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ObjectiveSelect) Aggregate(fns ...AggregateFunc) *ObjectiveSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ObjectiveSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ObjectiveQuery, *ObjectiveSelect](ctx, _s.ObjectiveQuery, _s, _s.inters, v)
}

func (_s *ObjectiveSelect) sqlScan(ctx context.Context, root *ObjectiveQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
