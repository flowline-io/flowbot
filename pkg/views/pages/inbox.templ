package pages

import (
	"context"
	
	"github.com/flowline-io/flowbot/pkg/views/layout"
	"github.com/flowline-io/flowbot/pkg/views/partials"
)

// InboxPage renders the user notification inbox.
templ InboxPage(ctx context.Context, filter string) {
	@layout.Base(ctx, DocTitlePage(ctx, "page.inbox.title")) {
		@partials.PageHeader(ctx, "page.inbox.title", "page.inbox.subtitle") {
		}
		<div
			hx-get={ templ.URL(partials.InboxListURL(filter)) }
			hx-trigger="load, every 30s"
			hx-swap="outerHTML"
			data-testid="inbox-panel">
			@partials.PanelSkeleton(ctx)
		</div>
	}
}
