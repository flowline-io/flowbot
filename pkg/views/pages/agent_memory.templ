package pages

import (
	"context"
	
	"github.com/flowline-io/flowbot/pkg/i18n"
	"github.com/flowline-io/flowbot/pkg/types/model"
	"github.com/flowline-io/flowbot/pkg/views/layout"
	"github.com/flowline-io/flowbot/pkg/views/partials"
)

templ AgentMemoryPage(ctx context.Context, items []model.AgentMemoryFact, scope string) {
	@layout.Base(ctx, DocTitleFlowbot(ctx, "nav.memory_facts")) {
		@partials.PageHeader(ctx, "page.agent_memory.title", "page.agent_memory.subtitle") {
			<form class="inline-flex items-center gap-2 m-0"
				hx-get="/service/web/agent-memory/list"
				hx-target="#agent-memory-table"
				hx-swap="outerHTML"
				hx-trigger="submit">
				<input type="text" name="scope" value={ scope }
					placeholder="scope"
					data-testid="agent-memory-scope"
					class="input input-bordered input-sm w-40"/>
				<button type="submit" data-testid="agent-memory-refresh" class="btn btn-ghost btn-sm">
					{ i18n.T(ctx, "common.refresh") }
					@partials.HtmxIndicator()
				</button>
			</form>
		}
		@partials.AgentMemoryTable(ctx, items, scope)
	}
}
