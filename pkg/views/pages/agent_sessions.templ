package pages

import (
	"context"
	
	"github.com/flowline-io/flowbot/pkg/i18n"
	"github.com/flowline-io/flowbot/pkg/types/model"
	"github.com/flowline-io/flowbot/pkg/views/layout"
	"github.com/flowline-io/flowbot/pkg/views/partials"
)

templ AgentSessionsPage(ctx context.Context, items []model.AgentSession, nextCursor string) {
	@layout.Base(ctx, DocTitleFlowbot(ctx, "nav.sessions")) {
		@partials.PageHeader(ctx, "nav.sessions", "") {
			<button hx-get="/service/web/agent-sessions/list"
				hx-target="#agent-sessions-table"
				hx-swap="outerHTML"
				data-testid="agent-sessions-refresh"
				class="btn btn-ghost btn-sm">
				{ i18n.T(ctx, "common.refresh") }
				@partials.HtmxIndicator()
			</button>
		}
		@partials.AgentSessionTable(items, nextCursor)
	}
}
