package partials

templ ViewMarkdownContent(content templ.Component) {
	<div class="card bg-base-100 shadow-sm card-body prose prose-sm max-w-none text-base-content" data-testid="view-markdown-content">
		@content
	</div>
}
