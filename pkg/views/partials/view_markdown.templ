package partials

templ ViewMarkdownContent(content templ.Component) {
	<div class="flowbot-surface card-body prose prose-sm max-w-none text-base-content" data-testid="view-markdown-content">
		@content
	</div>
}
