package partials

import (
	"github.com/flowline-io/flowbot/pkg/types/model"
)

templ AgentSessionEntriesTable(entries []model.AgentSessionEntry) {
	<div class="flowbot-surface" id="agent-session-entries-table" data-testid="agent-session-entries-table">
		<div class="overflow-x-auto">
			<table class="table">
				<thead>
				<tr>
					<th class="text-xs text-base-content/55">Time</th>
					<th class="text-xs text-base-content/55">Entry ID</th>
					<th class="text-xs text-base-content/55">Type</th>
					<th class="text-xs text-base-content/55">Parent ID</th>
					<th class="text-xs text-base-content/55">Payload</th>
				</tr>
				</thead>
				<tbody>
				for _, entry := range entries {
					@AgentSessionEntryRow(entry)
				}
				if len(entries) == 0 {
					<tr>
						<td colspan="5" class="text-center text-base-content/50 py-4">No entries in this session.</td>
					</tr>
				}
				</tbody>
			</table>
		</div>
	</div>
}
