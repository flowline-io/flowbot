package partials

import "github.com/flowline-io/flowbot/pkg/types/model"

templ AgentSubagentTable(items []model.AgentSubagent) {
	<div class="flowbot-surface">
		<div id="agent-subagents-table"
			data-testid="agent-subagents-table"
			class="overflow-x-auto">
			<table class="table">
				<thead>
				<tr>
					<th class="text-xs text-base-content/55">Flag</th>
					<th class="text-xs text-base-content/55">Name</th>
					<th class="text-xs text-base-content/55">Description</th>
					<th class="text-xs text-base-content/55">Model</th>
					<th class="text-xs text-base-content/55 text-center">Enabled</th>
					<th class="text-xs text-base-content/55">Updated</th>
					<th class="text-xs text-base-content/55">Actions</th>
				</tr>
				</thead>
				<tbody id="agent-subagents-rows">
				for _, item := range items {
					@AgentSubagentRow(item)
				}
				if len(items) == 0 {
					<tr id="agent-subagents-empty">
						<td colspan="7" class="text-center text-base-content/50">No agent subagents found.</td>
					</tr>
				}
				</tbody>
			</table>
		</div>
	</div>
}
